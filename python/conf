begin
  button = KEY_POWER
  prog = pylirc
  config = KEY_POWER
end

begin
  button = KEY_MODE
  prog = pylirc
  config = KEY_MODE
end

begin
  button = KEY_MUTE
  prog = pylirc
  config = KEY_MUTE
end

begin
  button = KEY_PLAYPAUSE
  prog = pylirc
  config = KEY_PLAYPAUSE
end

begin
  button = KEY_PREVIOUS
  prog = pylirc
  config = KEY_PREVIOUS
end

begin
  button = KEY_NEXT
  prog = pylirc
  config = KEY_NEXT
end

begin
  button = KEY_EQUAL
  prog = pylirc
  config = KEY_EQUAL
end

begin
  button = KEY_VOLUMEDOWN
  prog = pylirc
  config = KEY_VOLUMEDOWN
end

begin
  button = KEY_VOLUMEUP
  prog = pylirc
  config = KEY_VOLUMEUP
end

begin
  button = KEY_0
  prog = pylirc
  config = KEY_0
end

begin
  button = KEY_BACK
  prog = pylirc
  config = KEY_BACK
end

begin
  button = KEY_ENTER
  prog = pylirc
  config = KEY_ENTER
end

begin
  button = KEY_1
  prog = pylirc
  config = KEY_1
end

begin
  button = KEY_2
  prog = pylirc
  config = KEY_2
end

begin
  button = KEY_3
  prog = pylirc
  config = KEY_3
end

begin
  button = KEY_4
  prog = pylirc
  config = KEY_4
end

begin
  button = KEY_5
  prog = pylirc
  config = KEY_5
end

begin
  button = KEY_6
  prog = pylirc
  config = KEY_6
end

begin
  button = KEY_7
  prog = pylirc
  config = KEY_7
end

begin
  button = KEY_8
  prog = pylirc
  config = KEY_8
end

begin
  button = KEY_9
  prog = pylirc
  config = KEY_9
end
